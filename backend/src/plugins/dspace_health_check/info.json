{
  "name": "dspace_health_check",
  "display_name": "DSpace Health Check",
  "description": "To remove duplicates and add missing items.",
  "multiple": "true",
  "source": [
    "handle"
  ],
  "params": [
    {
      "name": "repo",
      "label": "Repository name",
      "type": "select",
      "items": "repositoriesList",
      "required": true
    },
    {
      "name": "itemEndPoint",
      "label": "Link to DSpace REST API endpoint (Handle/UUID as per the repository sitemap structure)",
      "type": "text",
      "required": true
    },
    {
      "name": "sitemapIdentifier",
      "label": "Item identifier in the repository sitemap structure",
      "type": "select",
      "items": ["handle", "uuid"],
      "required": true
    }
  ]
}