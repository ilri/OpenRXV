{
    "name": "dspace_add_missing_items",
    "display_name": "DSpace Add Missing Items",
    "description": "DSpace add missing items (this is used by the health check plugin, do not activate manually).",
    "multiple": "false",
    "params": [
        {
            "name": "repo",
            "label": "Repository name",
            "type": "text",
            "required": true
        }
    ]
}
